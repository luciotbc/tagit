<!DOCTYPE html>
<html>
<head>
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title><%= content_for?(:title) ? yield(:title) : "Tag-It" %></title>
	<meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Tag-It" %>">
	<%= stylesheet_link_tag "application", :media => "all" %>
	<%= javascript_include_tag "application" %>
	<%= csrf_meta_tags %>
	<%= yield(:head) %>
</head>
<body>
	<%= render 'layouts/navigation' %>
	<div id="main" role="main">
		<div class="container">
			<%= render 'layouts/messages' %>
			<div class="content">
				<%= yield %>
			</dv>
		</div>
	</div>
</body>
</html>
